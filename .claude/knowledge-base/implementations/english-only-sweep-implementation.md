# Implementation: English-only sweep — Fases 0 e 1

> **Generated by `/implement english-only-sweep`** — contrato de trabalho do halt-loop, derivado de `.claude/knowledge-base/plans/english-only-sweep-plan.md` (v1.2).

**Slug:** `english-only-sweep`
**Source plan:** `.claude/knowledge-base/plans/english-only-sweep-plan.md`
**Plan verdict:** `SHIPPABLE` (score 90.4, zero hard caps)
**Working branch:** `workspace` (Regra Inquebrável 4)
**Started:** 2026-08-05

## Escopo desta invocação — Fases 0 e 1 APENAS

Decisão do usuário em 2026-08-05: executar **somente** as fases 0 e 1. As fases 2–6 aguardam as cinco perguntas abertas (`## Unresolved Questions` do plano), que bloqueiam T2.1 (Q2), T4.2 (Q3), T6.1 (Q5), T6.2 (Q4) e T6.3 (Q1).

Isto **não** é um recorte de conveniência: as fases 0 e 1 são exatamente as que o plano defende como pré-requisito de qualquer tradução — o portão que impede a regressão, e os três guardas que a tradução destruiria em silêncio. Entregá-las isoladamente deixa o repositório num estado coerente: mais protegido do que antes, sem nenhuma string traduzida ainda.

Tarefas fora deste escopo **não** são marcadas `blocked` — elas simplesmente não entram nesta invocação. `blocked` é reservado para tarefa tentada e travada, e usá-lo aqui reportaria um impedimento que não existe.

## Pre-condition audit (verificado em 2026-08-05)

- [x] Plan verdict ≥ SHIPPABLE_WITH_CAVEATS — `SHIPPABLE` (90.4)
- [x] On `workspace` — `git branch --show-current` → `workspace`
- [x] No uncommitted changes at start — `git status --porcelain` vazio em `225e531`
- [x] Toolchain pronto — `package.json` + `pnpm-lock.yaml`; Node v22.22.2 satisfaz `engines.node >=20.0.0`; sem `.nvmrc` a travar versão
- [x] Nenhum ralph-loop ativo — sem `ralph-loop.local.md`
- [x] TDD shape gate — `check_tdd_shape.py` exit 0 (14/14 tarefas com forma executável)
- [ ] **SEPA NÃO gerado** — o Step 2.5 da SKILL exige invocar o `Agent`, e a configuração desta sessão proíbe chamá-lo sem pedido explícito do usuário. Registrado como desvio consciente do contrato da skill, não como esquecimento. Consequência honesta: esta execução não terá a segunda opinião 3×/iteração; os portões automáticos (wiring, mini review, validation) permanecem intactos.

## Task list

| # | Plan ref | Status | Wiring (a) | Wiring (b) | Wiring (c) | Commit SHA |
|---|---|---|---|---|---|---|
| T0.1 | Phase 0 / T0.1 | pending | — | — | — | — |
| T1.1 | Phase 1 / T1.1 | pending | — | — | — | — |
| T1.2 | Phase 1 / T1.2 | pending | — | — | — | — |
| T1.3 | Phase 1 / T1.3 | pending | — | — | — | — |

Status legend: `pending` / `red` / `green` / `refactor` / `wired` / `committed` / `blocked`
Wiring legend: `✓` passou · `✗` falhou · `defer` adiado por ADR · `—` não checado

## Ordem e dependências

```
T0.1 (portão + catraca)  ──▶  T1.1 ─┐
                                    ├──▶  fim desta invocação
                              T1.2 ─┤
                              T1.3 ─┘
```

T0.1 vem primeiro porque T1.3 escreve seus testes em `tests/repo/`, que só é coletado depois que T0.1 estende o `include` do `vitest.workflows.config.ts`. T1.1 e T1.2 são independentes entre si e de T1.3.

## Per-task contract

### T0.1 — Portão de idioma com catraca por tier

**Plan reference:** `.claude/knowledge-base/plans/english-only-sweep-plan.md` § Phase 0 / T0.1

**Arquivos:** `tests/repo/language-gate.test.ts` (NEW) · `tests/repo/language-budget.json` (NEW) · `vitest.workflows.config.ts` · `package.json` · `.github/workflows/ci.yml`

**RED (11 casos, do plano):** `gate_fails_when_a_tier_grows`, `gate_fails_when_budget_overstates_debt`, `gate_ignores_expired_carve_out`, `gate_reports_path_and_line`, `ratchet_reads_merge_base_not_head`, `ratchet_skips_loudly_when_base_unresolvable`, `detector_matches_unaccented_portuguese`, `carve_out_with_invalid_sunset_is_rejected`, `carve_out_without_issue_is_rejected`, `gate_fails_clearly_when_budget_is_malformed`, `gate_skips_missing_file_without_aborting`

**Asserção-âncora:** `expect(tierDFilenameCount).toBe(2)` — antes da varredura; 0 depois.

**Invariantes que não podem quebrar:** `pnpm run test:workflows` continua coletando `tests/workflows/gates.test.ts`; a catraca lê o merge-base, nunca `HEAD`; carve-out com `sunset` ou `issue` inválidos é rejeitado, nunca interpretado.

**Wiring:** pillar (a) — o portão é um teste, e seu "caller" é o runner (`vitest.workflows.config.ts` + o script `test:repo` chamado por `ci.yml`). Pillars (b) e (c) — N/A declarado: não há boundary externo nem métrica de runtime nesta tarefa.

**VERIFY:** `pnpm run test:repo`

---

### T1.1 — Asserção do boot do MCP deixa de depender do literal PT

**Plan reference:** § Phase 1 / T1.1

**Arquivos:** `packages/mcp/tests/contract/bin.contract.test.ts`

**RED:** teste alterado passa contra o `bin.ts` **em português**; falha contra um `bin.ts` injetado que cita as duas variáveis; `assertion_is_scoped_to_the_error_line` (EC-15).

**Asserção-âncora:** `expect(errLineMentionsRegistry).toBe(false)`.

**Invariante:** continua provando que o stdio sem credencial nomeia **só** `THEOSKILL_AUTH`. `bin.ts` **não** é tocado aqui — a ordem é o ponto da tarefa.

**Wiring:** pillars (a)/(b)/(c) — N/A; a mudança é o próprio teste de contrato.

**VERIFY:** `pnpm --filter @usetheo/skills-mcp test`

---

### T1.2 — Cardinalidade de hints travada nos ramos descobertos

**Plan reference:** § Phase 1 / T1.2

**Arquivos:** `packages/core/src/domain/discoverability.test.ts`

**RED:** `hints.push` duplicado injetado faz os 5 casos falharem; `all_three_causes_yield_exactly_three_unique_hints` (EC-10); `draft_with_no_cause_has_zero_causes_and_one_hint` (EC-11).

**Asserção-âncora:** `expect(uniqueHintCount).toEqual(hintCount)`.

**Invariante:** o ramo do rascunho conserva `toHaveLength(1)` com `causes.length === 0` — a fórmula geral não se aplica ali. `discoverability.ts` **não** muda; se alguma asserção nova falhar, é defeito preexistente e vira issue, não acomodação.

**Wiring:** N/A — mudança em teste.

**VERIFY:** `pnpm --filter @usetheo/skills test`

---

### T1.3 — Snapshot da superfície de API pública

**Plan reference:** § Phase 1 / T1.3

**Arquivos:** `tests/repo/api-surface.test.ts` (NEW) · `tests/repo/core-api-surface.json` (NEW) · `tests/repo/sdk-api-surface.json` (NEW) · `tests/repo/core-api-surface.d.ts` (NEW) · `.github/workflows/publish-npm.yml`

**RED:** `surface_test_fails_when_a_symbol_is_renamed`, `surface_test_fails_when_a_symbol_is_added`, `surface_resolver_throws_beyond_depth_2`, `dts_snapshot_fails_when_an_exported_FIELD_is_renamed` (EC-5), `dts_snapshot_fails_loudly_when_dist_is_missing`.

**Asserção-âncora:** `expect(coreSurface).toEqual(coreSurfaceSnapshot)`.

**Invariantes:** os snapshots commitados refletem o estado **pré-rename** (em português) — este é o ponto da tarefa; o teste do `.d.ts` falha pedindo `pnpm build` quando `dist` não existe, nunca é pulado em silêncio.

**Depende de:** T0.1 (o `include` de `tests/repo/**` precisa existir).

**Wiring:** pillar (a) — o runner + o step de `publish-npm.yml`. (b)/(c) N/A.

**VERIFY:** `pnpm build && pnpm run test:repo`

## Followups (registrar, nunca incluir no commit corrente)

Arquivo: `.claude/knowledge-base/implementations/english-only-sweep-followups.md`
