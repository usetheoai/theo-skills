---
milestone_id: M32
verdict: ACCEPTED
target: https://app-dev.usetheo.dev (dashboard d2fe51e · registry theo-skills v0.15.0)
date: 2026-08-04
---

# Aceitação — M32, ciclo de vida da skill (fechamento pela tela)

Fecha o que o [record anterior](M32-2026-08-04.md) deixou aberto. Aquele run validou AC1–AC4
contra o build da tag `v0.13.0` e registrou **AC5 como `not_exercised`**, honestamente: a tela não
existia, e o `CLAUDE.md` deste repositório proíbe frontend aqui.

A tela nasceu no `theo-cloud/dashboard` (PR #423, mergeado) e está no ar. Este run a exercita
**por clique** e re-mede os outros quatro critérios contra o sistema vivo — não contra o build
local que os validou da primeira vez.

## Alvo

`https://app-dev.usetheo.dev`, servindo o dashboard `d2fe51e` (`built_at 2026-08-04T16:54Z`,
lido em `GET /v1/version`) contra o registry publicado. Nenhum critério foi exercitado contra
árvore de trabalho, mock ou `pnpm dev`.

## AC1 — Duas dimensões ortogonais — `passed`

Re-medido **ao vivo**, não só no build da tag:

```
GET /v1/dashboard/skills/m26-verify-clique → 200
{..., "lifecycle":"deprecated", "enabled":true, ...}
```

A skill está **descontinuada e habilitada ao mesmo tempo** — a combinação que só existe se os
eixos não colapsaram num enum só.

## AC2 — Deprecada continua resolvível — `passed`

A tela de detalhe da skill deprecada renderiza **"Instrução resolvida"** com o texto completo do
frontmatter. Quem já a referencia continua carregando a instrução; o que mudou é a descoberta.

## AC3 — A descoberta exclui por padrão, com opt-in explícito — `passed`

A prova mais forte que o acervo permite: **a consulta é literalmente a descrição da skill
deprecada**, e ela não volta.

| | |
|---|---|
| Skill deprecada | `m26-verify-clique` |
| Descrição dela | *"Skill DE TESTE criada para verificar a promocao de canal pela tela (M26)."* |
| Consulta usada | `verificar promocao de canal pela tela` |

```
GET /v1/dashboard/skills/:retrieve?query=verificar+promocao+de+canal+pela+tela → 200
results: [ lt020-1785621219 (0.0167), redteam-cambio-1785615135 (0.0164) ]
```

A deprecada está **ausente** apesar de casar melhor que as duas devolvidas. O opt-in aparece na
tela como caixa de seleção — `Incluir descontinuadas`, ao lado de `Incluir rascunhos`.

O caveat do run anterior (o anúncio da quebra não precedeu o novo default) permanece registrado
em **#134** e não é revertido por este run: é um fato de processo, não de código.

## AC4 — Motivo e sucessora viajam no contrato — `passed`

O motivo não só viaja como **chega à tela**: sob o estado, a linha
`Motivo: skill de teste do M26 — substituida pelas jornadas do M31`.

A sucessora (`superseded_by`) foi validada contra a tag, com as duas recusas que importam —
auto-referência e sucessora inexistente (`handlers/lifecycle.ts:128-148`).

## AC5 — A tela diz o que deixa de valer — `passed`

Era o critério aberto. Exercitado por clique, ponta a ponta:

```
PUT /v1/dashboard/skills/m26-verify-clique/lifecycle → 200
```

O `200` também é a prova de produção do **ADR 0003**: o gate era `requireRole('admin')`, que
resolve o papel na tabela de membros pelo `userId`, e a credencial do broker carrega
`sys_platform_gateway` — usuário sintético, **não-membro de propósito**. O painel recebia 403 em
todo pedido. Trocado para escopo, porque depreciar é curadoria.

O que a tela faz, contra o `DESIGN.md` §2.2:

- **Diz o que NÃO muda antes de agir** — *"Descontinuar **não remove** a skill nem quebra quem já a referencia… O que muda é a **descoberta**."* Um aviso que descreve a consequência real, não "o item será alterado".
- **Estado em texto, nunca só cor** — `DESCONTINUADA`, e o motivo logo abaixo.
- **A alternativa reversível vem antes da destrutiva** — "Voltar a circular" no card de ciclo de vida; "Apagar esta skill" isolado na Zona de perigo, que declara que **reserva o identificador** e que **não há como desfazer**.
- **Frase digitada** — o `ConfirmDialog` exige digitar o identificador.

## Verdict

```
compute_acceptance_verdict.py --criteria m32-criteria.json --evidence m32-evidence.json
  - all 5 criteria exercised and evidenced in the live system.
ACCEPTED
```

Computado pelo script, não afirmado por mim.

## Evidência

- `evidence/M32-AC2-AC5-deprecada-resolve.png`
- `evidence/M32-AC3-retrieve-exclui-deprecada.txt`
- `evidence/M32-2026-08-04-tag-v0.13.0.txt` (run anterior, AC1/AC4)

## Uma coisa que quase reportei errado

Depois do `PUT … → 200`, o snapshot da tela ainda mostrava `EM USO`. Meu primeiro impulso foi
tratar como defeito de renderização. Medi antes: o corpo do refetch trazia
`"lifecycle":"deprecated"` e o snapshot seguinte mostrava `DESCONTINUADA` com o motivo e o botão
"Voltar a circular". Era snapshot tirado antes do re-render — a **terceira vez** que esse mesmo
falso alarme apareceu nesta sessão.

Não custou nada porque foi medido. Teria custado um issue falso se eu tivesse confiado no
primeiro sinal.
