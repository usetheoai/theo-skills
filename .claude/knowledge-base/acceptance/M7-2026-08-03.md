---
milestone_id: M7
verdict: NOT_VALIDATED
target: "npm: @usetheo/skills@0.1.0, @usetheo/skills-sdk@0.1.0, @usetheo/skills-mcp@0.1.0"
date: 2026-08-03
---

# Aceitação M7 — `NOT_VALIDATED`

**Veredito computado por `compute_acceptance_verdict.py`**, não nomeado por mim. Checkbox
permanece `[ ]`.

> **Atualizado no fim da sessão**, após correções reais: AC2 exercitado (era `not_exercised`),
> AC5 retratado (o defeito era meu, não do produto), CLI corrigido e publicado, AC4 medido como
> alcançável. Recomputado: **`NOT_VALIDATED`** — dois critérios não exercitados em vez de quatro,
> e um reprovado. Não é re-execução sem correção: o que mudou está em `bb00847`, `d9b56b2`,
> `f2052b8`, `414bc96`, `bf2eade`.

## Alvo

Pacotes **publicados** hoje (`releases/m7-npm-first-publish-2026-08-03.md`). O
`@usetheo/skills-cli` **não** foi publicado — declara `@usetheo/skills-api` (`private: true`) em
`dependencies`; medido no tarball, publicaria `skills-api@0.0.0` e `npm i` quebraria com `E404`
**permanentemente**.

## Resultado por critério

| # | Status | Evidência / razão |
|---|---|---|
| AC1 | **passed** | `createRemoteSkillsManager` existe e é função, no pacote publicado |
| AC2 | **passed** | forma medida contra `@theokit/sdk` **4.37.0** real; `Skill.create` existe. Não invocado — exige credencial |
| AC3 | **passed** | `npm view` → `0.1.0`; import externo resolveu de `…/ac3/node_modules/…` — **fora do repo** |
| AC4 | not_exercised | **registry NO AR** (`401`, não `000`) — falta chave `skills:read` por inquilino, **pedida ao humano** |
| AC5 | **passed** ¹ | fallback engajou em 0 ms (≤5 s) e **`isDegraded() === true`** — ver retratação |
| AC6 | **failed** | **1 data** distinta (exige ≥3) e **0** `outcome: fail` (exige ≥1). Âncora e manifesto **criados hoje** — não existiam |
| AC7 | not_exercised | **zero consultas de usuário registradas**; exercitá-lo seria eu escrevê-las — o que o critério proíbe |

## RETRATAÇÃO ¹ — o defeito do AC5 NÃO EXISTE, o erro foi meu

Reportei que `usedFallback` não ficava `true`. **`usedFallback` não existe** — inventei o nome ao
ler o comentário da interface (*"`true` quando a última resolução veio do fallback"*) e o usei
duas vezes: no teste **e na DoD que eu mesmo escrevi horas antes**. A API real é `isDegraded()`,
e ela devolve `true`: `{"ms":0,"isDegraded":true,"n":1}`.

O produto está correto. Eu afirmei um defeito de severidade **major** que não existe — a mesma
falha das duas anteriores desta sessão (`RemoteSkillsManager`, "não é executável daqui"): concluir
a partir de um nome suposto em vez de medido.

## O defeito que esta execução encontrou (um, não dois)

**As três evidências de dogfood que o roadmap afirmava não existem (AC6).** O texto do M7 dizia
*"Três evidências existem desde 2026-07-31 e agora CONTAM"*. Medido: **nenhum** `.md` sob qualquer
caminho `dogfood/evidence`, e `git log --all` não mostra arquivo algum lá — nunca estiveram sob
git. O `status: running` é inalcançável sem elas, e o AC7 depende do AC6. Severidade: **blocker**.

## Erro meu nesta execução, registrado

A primeira tentativa do AC5 usou `fallback:` como opção; a real é **`localFallback:`** (array, não
função). Aquela execução devolveu `n:0` e eu quase a li como "o fallback não funciona". Refiz com o
nome correto — o resultado acima é o da execução válida. O defeito do `usedFallback` sobrevive à
correção; o `n:0` não.

## Por que `NOT_VALIDATED` e não `REJECTED`

Há critérios `failed` **e** critérios nunca exercitados. "Não consegui verificar" e "verifiquei e
está quebrado" são fatos diferentes, e o script separa os dois. Ambos bloqueiam o flip igualmente.

## Segunda re-execução (fim da sessão)

Após construir o aparato de dogfood (`6c8c573`) e registrar a inalcançabilidade do AC7 com
evidência. Verdicto recomputado: **`NOT_VALIDATED`**. Quatro critérios passados, um reprovado,
dois não exercitados — cada um com a razão medida, nenhum presumido.

## O que trava, ao fim da sessão

| bloqueio | natureza | quem resolve |
|---|---|---|
| **AC4** | falta chave `skills:read` por inquilino; o registry responde | **humano** — pedida, não cunhada por mim (exige admin; chaves órfãs já ocorreram nesta corrida) |
| **AC6** | tem **1** das ≥3 datas e **0** das ≥1 histórias de falha | **calendário** — faltam 2 dias de uso real |
| AC7 | depende do AC6 por construção | — |

## Próximo passo

AC6 é o caminho crítico e **não fecha por commit**: são ≥3 evidências em ≥3 datas distintas, com
≥1 `outcome: fail`. AC5 é corrigível já (fazer o `usedFallback` refletir a degradação) e deve
entrar como fix antes da próxima aceitação.
