---
milestone_id: M30
verdict: ACCEPTED
target: "serviço do build 0.2.0 + postgres real; pacotes npm @usetheo/* 0.2.0"
date: 2026-08-03
---

# Aceitação M30 — `ACCEPTED`

Veredito **computado** por `compute_acceptance_verdict.py`: *"all 5 criteria exercised and
evidenced in the live system"*.

| # | Status | Evidência |
|---|---|---|
| AC1 | **passed** | `invalid_zip` idêntico em `:validate` e `POST` — mesmo `ingestPayload` |
| AC2 | **passed** | `GET /v1/skills` 0 → 3× `:validate` com payload **válido** → 0 |
| AC3 | **passed** | `skillMd` avulso → `{ok, name, description, execution}` extraídos |
| AC4 | **passed** | `{error, message, field}` no HTTP; e `field`/`line` do **pacote publicado** |
| AC5 | **passed** | `401` com auth ligada, payload válido, sem bearer |

## Re-execução pelo caminho prescrito (mesmo dia)

A primeira execução subiu o serviço do build da **working tree**. O `cycle-acceptance` exige
build do artefato liberado — *"never the dirty working tree"*. Refeito por `git worktree`:

**Achado que teria invalidado a re-execução:** a tag `v0.2.0` do repositório é de **2026-06-23**,
tem `version: 0.0.0` no core e **não contém a rota `:validate`** (`grep -c` = 0). É tag
preexistente de outro corte. Exercitar contra ela daria `ACCEPTED` sobre código que não é o do
M30 — aceitação sobre o artefato errado, com aparência de rigor. Usei o commit real do release
(`2604fcf`), com `pnpm install --frozen-lockfile` e `pnpm -r build` isolados, contra o
`theoskill_pgvector`. Os cinco passaram de novo; evidência em `evidence/M30-worktree-tag.txt`.

> Consequência para o projeto: **a tag `v0.2.0` mente sobre o que contém.** O `/release` deste
> ciclo publicou no npm mas não cortou tag nova, e a antiga carrega o mesmo nome. Não corrigi —
> mexer em tag publicada é decisão do dono.

## Alvo

Serviço construído do build **0.2.0** contra postgres real com migrations aplicadas, e o pacote
`@usetheo/skills@0.2.0` instalado em diretório **fora do repositório** (`node_modules`, versão
confirmada) para o AC4.

## Duas correções de rota durante a execução, ambas medidas

**A primeira execução deu `NOT_VALIDATED`** com quatro critérios `not_exercised`: o serviço não
subia (`boot failed: connect ECONNREFUSED`) e a rota vive em `@usetheo/skills-api`, que é
`private: true`. Subi um postgres (`ankane/pgvector:v0.5.1`, comando documentado em
`benchmarks/README.md`), apliquei as migrations e o serviço subiu — o bloqueio era ambiente, não
produto.

**O AC5 deu `200` sem bearer na primeira medição.** A causa estava no log do próprio serviço:
`auth DESLIGADA (THEOSKILL_AUTH_REQUIRED != true) — o serviço não deve ser exposto assim`. Não era
defeito do critério: era a instância local com auth desligada. Religada, a rota devolve `401`.
Usei payload **válido** de propósito — um payload inválido daria `400` e não distinguiria "recusou
por auth" de "recusou por conteúdo".

## O que o AC1 provou, e o teste de contrato não conseguia

A igualdade de `code` entre `:validate` e `POST` estava registrada como limite no review: sem
banco, o `POST` morre em `internal_error` antes de validar o zip. Com banco real, os dois
devolvem `invalid_zip`. O caminho de ingestão é de fato único.
