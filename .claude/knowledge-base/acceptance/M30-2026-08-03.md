---
milestone_id: M30
verdict: NOT_VALIDATED
target: "npm: @usetheo/skills@0.2.0, -sdk, -mcp, -cli"
date: 2026-08-03
---

# Aceitação M30 — `NOT_VALIDATED`

Veredito **computado** por `compute_acceptance_verdict.py`. Checkbox permanece `[ ]`.

| # | Status | Razão |
|---|---|---|
| AC1 | not_exercised | rota em pacote `private`; serviço não sobe sem postgres |
| AC2 | not_exercised | idem |
| AC3 | not_exercised | idem |
| **AC4** | **passed** | `field`/`line` como dados, do pacote publicado `0.2.0`, fora do repo |
| AC5 | not_exercised | idem AC1 |

## O que impede — e é estrutural, não falta de tentativa

Quatro dos cinco critérios são sobre `POST /v1/skills:validate`. **A rota vive em
`@usetheo/skills-api`, que é `private: true` e nunca vai ao registry.** Não existe artefato
publicado que a contenha.

Tentei o caminho que o `cycle-acceptance` prevê para pacote interno — construir do tag e consumir
como o consumidor real. O serviço **exige banco no arranque**, mesmo para uma rota que não toca o
banco: `boot failed: connect ECONNREFUSED`. Sem o postgres do theo-skills no ar (só há o do
theo-promptly, outro banco), o consumo real é inalcançável nesta sessão.

Existem 5 testes de contrato exercitando a rota, com 4 mutações mortas. **Suíte de teste não é
aceitação** — o próprio `cycle-acceptance` nomeia isso como anti-padrão, e não vou renomeá-lo
para fechar um milestone.

## O que ISSO revela sobre a DoD

A DoD do M30 foi escrita — por mim, hoje — assumindo que a entrega seria alcançável via npm,
porque `acceptance-target.txt` declara publicação npm como o alvo do projeto. **Quatro dos cinco
critérios são sobre um pacote que, por contrato, nunca é publicado.**

Isso não é motivo para editar a DoD agora: seria a violação que o `cycle-goal` nomeia. É achado
para o dono decidir — ou o `skills-api` passa a ser publicável, ou o M30 precisa de um ambiente
com postgres para ser aceito, ou a DoD é revista conscientemente, fora de uma execução de
aceitação.

## O que foi entregue e está no ar

`@usetheo/skills` `-sdk` `-mcp` `-cli` em **0.2.0**, verificados com `npm view --no-cache`. O AC4
é exercitável e passa porque `parseFrontmatter` vive no `core`, que **é** publicado.
