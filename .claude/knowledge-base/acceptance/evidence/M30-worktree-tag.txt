# M30 — os cinco critérios a partir do BUILD DA TAG, nunca da working tree (2026-08-03)
#
# ACHADO QUE TERIA INVALIDADO A EXECUÇÃO: a tag `v0.2.0` do repo é de 2026-06-23, tem
# `version: 0.0.0` no core e NÃO CONTÉM a rota `:validate` (grep -c = 0). É tag preexistente
# de outro corte. Exercitar contra ela daria ACCEPTED sobre código que não é o do M30 —
# aceitação sobre o artefato errado, com aparência de rigor. Usei o commit real do release.

$ git worktree add <tmp> 2604fcf        # release(M30): 0.2.0 publicado
  versao core: 0.2.0 | :validate presente: 2 ocorrencias | arquivos sujos: 0
$ pnpm install --frozen-lockfile        # Done
$ pnpm -r build                         # 0 erros
$ pnpm -C packages/core db:migrate      # migrations applied  (theoskill_pgvector :5432)
$ node packages/api/dist/server.js      # do BUILD do worktree

# AC1 — mesmo `code` nas duas rotas, do MESMO ingestPayload
  POST /v1/skills:validate {"zippedFilesystem":""}   -> {"error":"invalid_zip"}
  POST /v1/skills          {"skill_id":"m30-wt",...} -> {"error":"invalid_zip"}

# AC2 — zero efeito colateral, por CONTAGEM
  GET /v1/skills -> 1 skill
  POST /v1/skills:validate × 3, payload VÁLIDO
  GET /v1/skills -> 1 skill        (inalterado)

# AC3 — SKILL.md avulso, sem ZIP
  {"ok":true,"name":"m30-worktree","description":"aceitacao do M30 a partir do build da tag",
   "execution":"remote"}

# AC4 — o erro diz ONDE
  {"error":"schema_invalid","message":"missing required field: name","field":"name"}
  (`error` preservado para compatibilidade; `message` e `field` acrescentados)

# AC5 — exige autenticação
  THEOSKILL_AUTH_REQUIRED=true -> {"auth_required":true}
  POST :validate, payload VÁLIDO, SEM bearer -> 401
