# AC4 — campo e linha no PACOTE PUBLICADO (2026-08-03)
$ cd /tmp/.../ac30 && npm init -y && npm i @usetheo/skills@0.2.0
found 0 vulnerabilities
$ ls -d node_modules/@usetheo/skills        -> node_modules/@usetheo/skills   (FORA do repo)
$ node -p "require('./node_modules/@usetheo/skills/package.json').version"  -> 0.2.0

$ node t.mjs      # parseFrontmatter importado do pacote publicado
{"code":"schema_invalid","field":"name","temMsg":true}
{"code":"schema_invalid","field":"category","line":4}

# `field` e `line` chegam como DADOS, do npm. O campo AUSENTE vem sem `line` — correto:
# campo que nao existe no arquivo nao tem posicao. Um `line` inventado ali mandaria o
# editor pintar a linha errada com confianca.
