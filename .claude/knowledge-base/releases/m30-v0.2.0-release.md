---
milestone_id: M30
version: 0.2.0
date: 2026-08-03
verdict: RELEASED
---

# Release 0.2.0 — a API da autoria

| pacote | `npm view --no-cache` |
|---|---|
| `@usetheo/skills` | **0.2.0** |
| `@usetheo/skills-sdk` | **0.2.0** |
| `@usetheo/skills-mcp` | **0.2.0** |
| `@usetheo/skills-cli` | **0.2.0** |

`@usetheo/skills-api` continua `private: true` — não vai ao registry por contrato.

## Verificação ANTES de publicar

Os quatro tarballs foram inspecionados com `pnpm pack`: nenhum arrasta o pacote privado. O CLI
já veio correto — a correção do M7 (`bb00847`) moveu `@usetheo/skills-api` para
`devDependencies`, e sem ela o `npm i` quebraria com `E404` permanente.

## Cache do registry

Logo após publicar, `npm view` ainda servia `0.1.0` em três dos quatro. Não é falha de
publicação — é propagação. Confirmado com `--no-cache`: os quatro em `0.2.0`. Registro porque
aceitar `0.1.0` como resultado teria me feito reportar uma publicação falhada que funcionou.
