# Rubric Opportunity v1 — Discover-Confidence M2 Structural

This file is the **source of truth** for the M2 structural rubric for opportunitys produced by `/discover-execute`. It defines:

1. The 4 DAG nodes (3 hard-cap dimensions + 1 penalty-only smell dimension) with weights summing to 1.0.
2. The spec-smell detection patterns (dictionaries + regexes), shared with `plan-confidence`'s rubric-v1.
3. Hard cap values for failure actions.

**v1.0 (2026-05-21):** Initial rubric, derived from `plan-confidence/templates/rubric-v1.md` but adapted for **opportunitys** (not implementation plans). The structural targets differ:

| Plan-confidence dimension | Discover-confidence equivalent |
|---|---|
| Coverage Matrix 100% (gaps → tasks) | Research Coverage 100% (4 corners covered) |
| ADR completeness (alternatives in Rationale) | Reference citations all exist (no fabrication) |
| TDD in bug-fix tasks | Opportunity completeness (all mandatory sections populated) |
| Spec smells (weak imperatives, loopholes) | Same |

## How to Evolve

When a new check is added (e.g., M3 citation faithfulness — does the cited path actually contain the claimed symbol?), DO NOT edit this file. Create `rubric-opportunity-v2.md` with the additional nodes and version 2.

## YAML Definition

```yaml
version: 1
created_at: 2026-05-21
updated_at: 2026-05-21
artifact_type: opportunity

nodes:
  - id: 1
    dimension: corner_coverage
    description: "All 4 corners populated: Evidence, Constraint Relation, Blast Radius, Verification. Each corner MUST have non-placeholder content; `unknown` with a reason counts ONLY for Constraint Relation."
    detector: corner_coverage
    failure_action: hard_cap
    hard_cap_value: 49
    weight: 0.30
  - id: 2
    dimension: evidence_pointers
    description: "Every code pointer `path:LINE` cited resolves on disk AND the line is within the file. Runtime observations are counted separately and never reported as verified. No fabricated evidence."
    detector: evidence_pointers
    failure_action: hard_cap
    hard_cap_value: 49
    weight: 0.30
  - id: 3
    dimension: opportunity_completeness
    description: "All mandatory sections present: Header, Item (B-NNN), Repo, Mode, Context, Corner 1-4, Recommendation. ADR required ONLY when the blast radius reaches beyond the opportunity's own repo."
    detector: opportunity_completeness
    failure_action: hard_cap
    hard_cap_value: 70
    weight: 0.25
  - id: 4
    dimension: structural_risk
    description: "Spec smells below threshold (subjective adjs, weak imperatives, vague pronouns, loopholes, non-verifiable terms)"
    detector: spec_smells
    failure_action: penalty
    penalty_weights:
      subjective_adjectives: -2
      weak_imperatives: -3
      vague_pronouns: -2
      loopholes: -3
      non_verifiable: -4
    weight: 0.15

smells:
  subjective_adjectives:
    pattern_type: dictionary
    words:
      - fast
      - efficient
      - robust
      - scalable
      - optimal
      - smart
      - friendly
  weak_imperatives:
    pattern_type: regex
    pattern: '\b(should|could|may|might)\b'
  vague_pronouns:
    pattern_type: regex
    pattern: '(^|\. )(This|That|These|Those)\b'
  loopholes:
    pattern_type: dictionary
    phrases:
      - "if possible"
      - "as appropriate"
      - "when applicable"
      - "where feasible"
  non_verifiable:
    pattern_type: dictionary
    words:
      - user-friendly
      - maintainable
      - scalable
      - robust
```

## Soft caps (conservative bias)

Beyond the hard caps above, soft caps may fire when signals indicate risk:

- `soft_floor_smell_density_high` — ≥ 20 weak-imperative / loophole / vague hits in prose (cap at 89).
- `soft_floor_citation_density_low` — < 1 `.claude/knowledge-base/references/` citation per 200 words of opportunity prose (cap at 89).
- `soft_floor_per_project_asymmetry` — one in-scope project gets > 80% of opportunity content while others get < 5% each (cap at 89). **Planned for M2.1; no Python checker exists yet — this cap is not fired by M2.**

Soft caps appear in `hard_caps_triggered` with prefix `soft_floor_` but do NOT trigger `verdict == INVALID`.

## Limitations (Known)

- **M3 future** — citation faithfulness (does the cited path actually contain the claimed behavior?) requires SAFE-adapted semantic check. M2 only verifies path existence.
- **EC analog** — smells inside `## Anti-patterns` or `## Blocked questions` sections of a opportunity are counted (false positives in metalanguage). Acceptable trade-off for M2.
- **Regex `\b` boundaries** — ASCII-oriented. Implementation uses `re.UNICODE` for safety.
