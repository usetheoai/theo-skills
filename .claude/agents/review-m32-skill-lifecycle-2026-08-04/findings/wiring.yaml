agent: review-m32-skill-lifecycle-wiring
review_target: 3b85e5f..HEAD for plan m32-skill-lifecycle
plan: .claude/knowledge-base/plans/m32-skill-lifecycle-plan.md
scope_note: |
  O intervalo pedido (3b85e5f..HEAD) EXCLUI o commit 3b85e5f, que é onde
  `packages/core/src/domain/skill-lifecycle.ts` nasceu. Os símbolos daquele arquivo foram
  verificados mesmo assim, porque o enunciado os lista explicitamente.
triad_revalidation:
  total_new_symbols: 9
  pillar_a_honest_pass: 8 / 9      # isValidLifecycle: check_wiring.py callers_count=0
  pillar_b_honest_pass: 8 / 9      # mesma exceção; check_wiring reporta FAIL para TODOS por
                                   # olhar `tests/integration/` na raiz — aqui é
                                   # `packages/api/tests/integration/`. Falso negativo do tool,
                                   # verificado à mão em cada símbolo.
  pillar_c_observed: 1 / 2         # `skill.lifecycle.changed` emitido e ASSERTADO em teste
                                   # (m32-lifecycle-operation.integration.test.ts:168);
                                   # a metade de DESCOBERTA não tem sinal algum.
findings:
  - id: F-wire-1
    severity: HIGH
    file: packages/api/src/server/store/skills-store.ts
    line: 143
    plan_ref: T5.1 Objective — "a ação existir na API, exigir motivo, e o contrato de leitura carregá-lo"
    summary: >-
      `deprecation_reason` e `superseded_by` são ESCRITOS e nunca LIDOS — nenhum caminho de
      leitura os devolve, então o consumidor recebe "deprecada" sem saber o porquê nem o que usar
      no lugar. É exatamente o 404-com-outro-nome que o handler diz estar evitando.
    evidence: |
      Escrita: handlers/lifecycle.ts:116-117 grava deprecationReason/supersededBy.
      Leitura: `toView` (skills-store.ts:113-140) e `liveColumns` (:143-160) não projetam
      lifecycle, deprecation_reason, superseded_by nem enabled. GET /v1/skills/:id devolve
      `getView` (skills.ts:327-333) — mesmos campos.
      grep -rn "deprecationReason|supersededBy" packages/ → único leitor é o próprio handler
      de escrita; o teste m32-lifecycle-discovery.integration.test.ts:89 confere o motivo por
      SQL direto, não pela API — sinal de que a API não o expõe.
      Precedente no MESMO arquivo (skills-store.ts:158): "`visibility` já era escrito e nunca
      devolvido: o operador mudava e não tinha como conferir" — o M32 reproduz o defeito.
    recommended_action: >-
      Projetar lifecycle/deprecation_reason/superseded_by no SkillView (liveColumns + toView) e
      no contrato de retrieve; teste de integração que deprecate via PUT e leia por GET.

  - id: F-wire-2
    severity: HIGH
    file: packages/api/src/server/handlers/skills.ts
    line: 320
    plan_ref: T4.1 — "`:retrieve` e `GET /v1/skills` aceitam as três flags"; Files to edit lista `handlers/skills.ts`
    summary: >-
      `GET /v1/skills` ficou fora do wiring: não lê as flags e não aplica filtro de ciclo de
      vida. Rascunhos e descontinuadas continuam listados normalmente — a garantia do
      milestone vale só em `:retrieve`.
    evidence: |
      grep -n "include_draft|include_deprecated|include_disabled|lifecycle"
        packages/api/src/server/handlers/skills.ts packages/api/src/server/store/skills-store.ts
        → zero ocorrências.
      git diff --name-status 3b85e5f..HEAD → skills.ts NÃO aparece (nem `handlers/lifecycle-flags.ts`,
      que o plano declarava NEW em Files-to-edit de T4.1).
      `buildLifecycleFilter` tem exatamente 1 caller de produção (retrieve.ts:69).
      listPaginated (skills-store.ts:105) filtra só state/deleted_at.
    recommended_action: >-
      Ou wirar as flags em GET /v1/skills, ou registrar ADR reduzindo o escopo de T4.1 a
      `:retrieve` — hoje o plano e o código discordam sem nota.

  - id: F-wire-3
    severity: HIGH
    file: packages/core/src/infrastructure/db/schema.ts
    line: 122
    plan_ref: T2.1 — coluna `enabled`; T4.1 — flag `include_disabled`
    summary: >-
      A coluna `enabled` não tem NENHUM caminho de escrita em produção — só leitura. Nada no
      produto consegue desligar uma skill, então `include_disabled=true` é um opt-in para um
      estado que a aplicação nunca produz.
    evidence: |
      grep -rn "enabled" packages/api/src packages/core/src (excluindo requireEnabled/
      include_disabled) → 2 hits: schema.ts:122 (definição) e
      domain/retrievers/lifecycle-clause.ts:33 (leitura `s.enabled = true`).
      handlers/lifecycle.ts:112-119 escreve lifecycle/deprecationReason/supersededBy — nunca `enabled`.
      Único escritor no repo: SQL cru do teste
      m32-lifecycle-endpoint.integration.test.ts:34 (INSERT ... enabled ...).
    recommended_action: >-
      Adicionar a operação de habilitação (PUT .../enabled ou campo no PUT .../lifecycle), ou
      remover coluna+flag deste milestone. Uma coluna cujo único escritor é um teste é
      infraestrutura sem caller.

  - id: F-wire-4
    severity: MEDIUM
    file: packages/api/src/server/handlers/retrieve.ts
    line: 114
    plan_ref: cycle-implement.md § Wiring triad — pilar (c) runtime metric
    summary: >-
      A metade de DESCOBERTA do M32 não tem sinal de runtime. O log `retrieve` não carrega as
      flags nem quantos resultados o ciclo de vida escondeu — quem opera vê `result_count` cair
      após o deploy e não tem como correlacionar com a mudança de default.
    evidence: |
      retrieve.ts:93-114 loga workspace_id, strategy, query_len, result_count, top_score,
      matched_vector, matched_keyword, latency_ms — nenhum campo de lifecycle, embora
      `lifecycle` seja calculado em :69 e passado ao retriever em :89.
      O único sinal novo do milestone é `skill.lifecycle.changed` (handlers/lifecycle.ts:137),
      que cobre a ESCRITA. Pilar (c) da leitura: ausente.
    recommended_action: >-
      Acrescentar ao log de `retrieve` as três flags (cardinalidade fixa, 3 booleanos) — mesmo
      padrão de baixa cardinalidade já justificado em retrieve.ts:96-104 para matched_*.

  - id: F-wire-5
    severity: MEDIUM
    file: .claude/knowledge-base/reviews/m32-skill-lifecycle-implement-validate-2026-08-04.md
    line: 29
    plan_ref: cycle-implement.md § Hard gates (post-halt-loop) — wiring summary re-verificado
    summary: >-
      O gate de wiring do /implement foi SKIP ("no progress file found — implement may not have
      been invoked"). A tríade nunca foi verificada por máquina antes deste review — e a
      re-execução manual achou uma falha real de pilar (a) que o gate teria pego.
    evidence: |
      Relatório de validação, seção `wiring_triad — SKIP`, motivo: "no progress file found".
      python3 .claude/skills/implement/scripts/check_wiring.py --symbol isValidLifecycle
        → verdict HALT, a_static_caller FAIL, callers_count 0.
      Nota honesta: o mesmo script reporta pilar (b) FAIL para TODOS os símbolos por procurar
      `tests/integration/` na raiz; neste monorepo os testes vivem em
      `packages/api/tests/integration/`. Esse FAIL é falso negativo do tool, não do código —
      verifiquei os 4 arquivos de integração à mão.
    recommended_action: >-
      Re-rodar o gate com o progress file presente, e corrigir o `check_wiring.py` para
      descobrir `**/tests/integration/` em layout de monorepo (senão o gate seguirá inútil aqui).

  - id: F-wire-6
    severity: MEDIUM
    file: packages/core/src/domain/skill-lifecycle.ts
    line: 55
    plan_ref: T1.1 Acceptance Criteria — vocabulário com erro tipado
    summary: >-
      `isValidLifecycle` não tem caller de produção — zero. Só o próprio teste unitário o
      exercita. Escapa da regra de export morto apenas porque `index.ts` faz `export *` em
      bloco, não por decisão deliberada de API pública.
    evidence: |
      grep -rn "isValidLifecycle" packages/ (sem /dist/) → 5 hits, todos em
      skill-lifecycle.ts (definição :55) e skill-lifecycle.test.ts (:6,:51,:53,:54).
      check_wiring.py --symbol isValidLifecycle → a_static_caller FAIL, callers_count 0,
      definition_only_excluded: [packages/core/src/domain/skill-lifecycle.ts].
      Exposto por packages/core/src/index.ts:111 (`export * from './domain/skill-lifecycle.js'`),
      não por export nomeado. Toda fronteira real usa `parseLifecycle`
      (handlers/lifecycle.ts:50) ou `buildLifecycleFilter` (retrieve.ts:69).
    recommended_action: >-
      Remover (parseLifecycle + try/catch já cobre o caso), ou dar-lhe o caller para o qual foi
      escrito. Manter export sem consumidor é dívida invisível — o teste dele testa a si mesmo.

  - id: F-wire-7
    severity: LOW
    file: packages/core/src/domain/retrievers/hybrid-retriever.ts
    line: 125
    plan_ref: T3.1 — o predicado entra na busca (as duas pernas)
    summary: >-
      Marcador `TODO` solto abrindo o comentário que documenta justamente a costura de wiring
      corrigida (`...params` atravessando para as duas pernas). O texto seguinte descreve
      trabalho CONCLUÍDO — o `TODO` é resíduo e sinaliza pendência onde não há.
    evidence: |
      hybrid-retriever.ts:125 — "// TODO filtro tem de atravessar para as DUAS pernas, e a
      lista é REPASSADA POR INTEIRO de propósito." O parágrafo abaixo narra o defeito já
      corrigido; o código em :126-129 já espalha `...params`.
      O wiring em si está correto e coberto por m32-lifecycle-endpoint.integration.test.ts
      (estratégia default `hybrid`).
    recommended_action: Trocar `// TODO filtro` por `// O filtro`.
notes_no_finding:
  - "Boundary/DIP: `domain/skill-lifecycle.ts` não importa infra, HTTP nem Zod; `schema.ts:17` importa domínio (direção infra→domínio, permitida); `lifecycle-clause.ts` fica no mesmo tier do `param-builder`. Sem violação."
  - "SQL injection: estágios vão por `b.bind()` em lifecycle-clause.ts:30 — parametrizados, nunca interpolados. Confere com o padrão do filtro de categoria."
  - "Pilar (c) da ESCRITA é honesto: `skill.lifecycle.changed` é emitido em handlers/lifecycle.ts:137 e ASSERTADO por m32-lifecycle-operation.integration.test.ts:168 com logger capturador — não é auto-declaração."
  - "Pilar (b) real: 4 suítes de integração exercitam schema, descoberta, endpoint HTTP e operação; o endpoint test bate na rota via `app.request` com as flags, não só no retriever."
